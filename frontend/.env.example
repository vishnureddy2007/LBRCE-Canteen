# Base URL of the Spring Boot backend.
# In dev: keep as localhost:8080 — the Vite proxy above also forwards /api
# and /uploads transparently, so you usually don't need to change this.
VITE_API_BASE_URL=http://localhost:8080/api